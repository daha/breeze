[{epa, [
       {topology, [{one, producer, debug_producer, 1, [{two, all}]},
		   {two, consumer, debug_consumer, 2, [{three, random}]},
		   {three, consumer, debug_consumer, 10, []}
		  ]
       }
      ]
}].
